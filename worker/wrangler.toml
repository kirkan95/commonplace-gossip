name = "commonplace-gossip"
main = "src/index.js"
compatibility_date = "2024-01-01"

[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "commonplace-gossip-audio"

# Secrets — set these via the Cloudflare dashboard or:
#   npx wrangler secret put PASSWORD
#   npx wrangler secret put TOKEN_SECRET
#   npx wrangler secret put ALLOWED_ORIGIN
